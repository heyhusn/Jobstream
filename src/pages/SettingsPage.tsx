import { ComingSoon } from "@/components/ui/ComingSoon";

export function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      note="Profile editing, resume versions, and data export/delete (M38) live here. The profile itself is already fully editable through onboarding — this page just needs a form around the same fields."
    />
  );
}
