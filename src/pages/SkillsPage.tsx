import { ComingSoon } from "@/components/ui/ComingSoon";

export function SkillsPage() {
  return (
    <ComingSoon
      title="Skill gaps"
      note="M12 from the roadmap — aggregating required skills across your matches, diffed against your profile. Needs the generation queue (cover letters, skill-gap narratives) built out first."
    />
  );
}
